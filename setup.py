from setuptools import setup, find_packages

setup(
    name="CiCADA experimentation",
    version="0.0.1",
    description="CiCADA",
    author="yamad07",
    packages=find_packages(),
    install_requires=[
        'torch',
    ],
)
